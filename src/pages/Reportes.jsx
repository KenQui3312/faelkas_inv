import { ReportesTemplate } from "../index";
// Importa el template visual ReportesTemplate, que contiene la estructura y diseño
// de la pantalla de reportes dentro del sistema.

export function Reportes() {
  // Componente funcional principal llamado "Reportes".
  // Su función es actuar como contenedor y renderizar el template correspondiente
  // a la pantalla de reportes del sistema.

  return (
    <>
      {/* Se renderiza el componente ReportesTemplate */}
      <ReportesTemplate />
    </>
  );
}
