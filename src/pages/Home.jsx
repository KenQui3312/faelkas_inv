import styled from "styled-components";
import { HomeTemplate} from "../index";
// Componente principal de la página Home
export function Home() {

  return (
    // Renderiza el template de la página de inicio
   <HomeTemplate/>
  );
}
